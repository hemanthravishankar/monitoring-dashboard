# Ignore all log files (e.g., app.log, debug.log)
*.log

# Ignore markdown files (e.g., README.md, CHANGELOG.md)
*.md

# Ignore go.sum if not needed during build (recalculated by `go mod tidy`)
# go.sum


# Optional additions:
.DS_Store                 # macOS hidden system files
.vscode/                  # VS Code editor settings
.idea/                    # JetBrains IDE project folder
*.iml                     # IntelliJ module files
npm-debug.log             # NPM debug logs
yarn-error.log            # Yarn error logs
.env.*                    # If using multiple env files like .env.development, .env.production
coverage/                 # If you're running tests with coverage reports
*.tsbuildinfo             # TypeScript incremental build info
build/                    # Some setups use `build` instead of `dist`